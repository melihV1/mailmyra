import { PageLoader } from '../../components/ui/PageLoader';

/** Giriş/kayıt/doğrulama ekranlarının yükleme durumu — panelle aynı dil. */
export default function AuthLoading() {
  return <PageLoader />;
}

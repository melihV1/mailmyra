# Monogram Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fotoğrafı olmayan kullanıcının imzasında, marka renginde bir baş-harf bloğu göstermek — böylece altı şablon da fotoğrafsızken düz metne çökmesin.

**Architecture:** Monogram bir `<table>`/`<td>`'dir, görsel değil (gerekçe: spec Karar 1 — Outlook dış görselleri engeller ve fallback'in kendisi engellenemez olmalı). Yeni `utils/monogram.ts` iki saf fonksiyon sunar; altı şablon kendi avatar yuvasında onu çağırır. `readableTextOn()` önce düzeltilir, çünkü monogram büyük bir renk bloğu üstüne metin koyar ve mevcut hâli varsayılan marka renginde 2.71 kontrast üretiyor.

**Tech Stack:** TypeScript, vitest. `packages/renderer` — framework bağımsız, DOM yok, React yok.

## Global Constraints

- **Renderer saf fonksiyondur.** DOM'a dokunma, React import etme, tarayıcı API'si kullanma (CLAUDE.md §İki Kalıcı Mimari Kural).
- **Çıktı table-based olmalı.** `<div>`, flexbox, grid, float, `position` YASAK.
- **Tüm CSS inline.** `<style>` bloğu yok.
- **Web-safe font.** Monogram fontu imzanın kendi `visuals.fontFamily`'sidir, sabit değil.
- **SVG, WebP, base64 gömülü görsel YASAK.**
- **Max genişlik ~600px** — monogram mevcut avatar kutusuna girdiği için bu sınır zaten korunuyor.
- Test komutu: `npm test -w packages/renderer`
- Tek test: `npx vitest run test/<dosya> -t "<isim>" --root packages/renderer`

---

## File Structure

| dosya | sorumluluk |
|---|---|
| `packages/renderer/src/utils/color.ts` | **değişir** — `readableTextOn` eşik yerine kontrast karşılaştırır |
| `packages/renderer/src/utils/monogram.ts` | **yeni** — `initialsFrom()`, `shouldShowMonogram()`, `monogramCell()` |
| `packages/renderer/src/types.ts` | **değişir** — `layout.monogram?: 'auto' \| 'off'` |
| `packages/renderer/src/templates/*.ts` (6) | **değişir** — avatar yuvasına monogram dalı |
| `packages/renderer/test/color.test.ts` | **değişir** — yanlış iddia düzeltilir, kontrast tabanı eklenir |
| `packages/renderer/test/monogram.test.ts` | **yeni** |
| `packages/renderer/test/guardrails.test.ts` | **değişir** — monogram modu eklenir |

`apps/web` HİÇ DEĞİŞMEZ. `'off'` anahtarının builder arayüzü ayrı iştir.

---

### Task 1: `readableTextOn()` — eşik yerine kontrast karşılaştırması

**Files:**
- Modify: `packages/renderer/src/utils/color.ts:26-29`
- Test: `packages/renderer/test/color.test.ts:27-37`

**Interfaces:**
- Consumes: `contrastRatio(a: string, b: string): number` — aynı dosyada, zaten var.
- Produces: `readableTextOn(hexBackground: string): '#ffffff' | '#000000'` — imza DEĞİŞMEZ, yalnız davranış düzelir. Altı şablon bunu `ctaText` için zaten çağırıyor.

**Neden:** Mevcut eşik `0.5`; siyah/beyaz geçiş noktası aslında `0.179`. Ürünün varsayılan marka rengi `#7b9fd3` bugün **beyaz** metin alıyor ve kontrast **2.71** — AA-large eşiğinin (3.0) bile altında. Düzeltmeyle siyah seçiliyor: **7.74**.

- [ ] **Step 1: Mevcut yanlış testi doğru beklentiye çevir ve kontrast tabanı ekle**

`test/color.test.ts` içindeki `describe('readableTextOn', ...)` bloğunu tamamen şununla değiştir:

```ts
describe('readableTextOn', () => {
  it('returns white text on a dark background', () => {
    expect(readableTextOn('#1a1a1a')).toBe('#ffffff');
  });
  it('returns black text on a light background', () => {
    expect(readableTextOn('#ffffff')).toBe('#000000');
  });
  // Eski test burada '#ffffff' bekliyordu ve YANLIŞTI: beyaz metin bu zeminde
  // 2.90 kontrast veriyor, AA-large eşiği olan 3.0'ın bile altında. Siyah 7.25.
  it('returns black text on the brand blue', () => {
    expect(readableTextOn('#719ad1')).toBe('#000000');
  });
  // Urunun VARSAYILAN marka rengi — regresyon kilidi.
  it('returns black text on the default brand colour', () => {
    expect(readableTextOn('#7b9fd3')).toBe('#000000');
  });
  // Asil garanti: hangi zemin gelirse gelsin secim AA normal metni gecer.
  it('never returns a choice below 4.5:1 contrast', () => {
    const samples = [
      '#f26b21', '#7a8b3c', '#6b8e9f', '#5c9e5c', '#8a8a8a',
      '#0057b8', '#ffd400', '#7b9fd3', '#719ad1', '#000000', '#ffffff',
    ];
    for (const bg of samples) {
      expect(contrastRatio(bg, readableTextOn(bg))).toBeGreaterThanOrEqual(4.5);
    }
  });
});
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

Run: `npx vitest run test/color.test.ts --root packages/renderer`
Expected: FAIL — `returns black text on the brand blue` ve `never returns a choice below 4.5:1 contrast` kırmızı.

- [ ] **Step 3: `readableTextOn`'u düzelt**

`packages/renderer/src/utils/color.ts` içindeki mevcut gövdeyi şununla değiştir:

```ts
/**
 * Verilen arka plan üstünde en okunur metin rengini (siyah/beyaz) döndürür.
 *
 * Luminans EŞİĞİ kullanmaz. Eskiden `> 0.5` eşiği vardı ve orta tonlu
 * markalarda kaybedeni seçiyordu: ürünün varsayılan `#7b9fd3`'ünde beyaz
 * metin 2.71 kontrast veriyordu (AA-large eşiği 3.0'ın bile altı), siyah
 * 7.74 verirdi. İki adayın kontrastını ölçüp büyüğünü seçmek her zaman
 * ≥ 4.58 verir — matematiksel en kötü hâl, ikisinin eşitlendiği
 * luminans 0.179 noktasıdır — yani AA normal metni de geçer.
 */
export function readableTextOn(hexBackground: string): '#ffffff' | '#000000' {
  return contrastRatio(hexBackground, '#ffffff') >= contrastRatio(hexBackground, '#000000')
    ? '#ffffff'
    : '#000000';
}
```

⚠️ `readableTextOn`, dosyada `contrastRatio`'dan ÖNCE tanımlı. Fonksiyon bildirimleri hoisted olduğu için sorun yok; sırayı değiştirme.

- [ ] **Step 4: Testlerin geçtiğini gör**

Run: `npx vitest run test/color.test.ts --root packages/renderer`
Expected: PASS (5 test)

- [ ] **Step 5: Tüm renderer testlerini koş — CTA rengi değiştiği için başka test kırılmış olabilir**

Run: `npm test -w packages/renderer`
Expected: PASS. Kırılan varsa: iddia `#ffffff` CTA metni bekliyorsa beklenti `#000000` olarak GÜNCELLENİR (davranış düzeldi, test eskiydi). Şablon kodunu değiştirme.

- [ ] **Step 6: Commit**

```bash
git add packages/renderer/src/utils/color.ts packages/renderer/test/color.test.ts
git commit -m "fix(renderer): pick readable text by contrast, not a luminance threshold

The default brand colour #7b9fd3 was getting white CTA text at 2.71:1,
below even the large-text threshold. Comparing both candidates' contrast
is always at least 4.58:1."
```

---

### Task 2: `initialsFrom()`

**Files:**
- Create: `packages/renderer/src/utils/monogram.ts`
- Test: `packages/renderer/test/monogram.test.ts`

**Interfaces:**
- Consumes: yok (saf string işlemi).
- Produces: `initialsFrom(fullName: string): string` — 0, 1 veya 2 karakter döner. Boş string "monogram basılamaz" demektir.

- [ ] **Step 1: Failing test yaz**

`packages/renderer/test/monogram.test.ts` oluştur:

```ts
import { describe, it, expect } from 'vitest';
import { initialsFrom } from '../src/utils/monogram';

describe('initialsFrom', () => {
  it('takes first and last word initials', () => {
    expect(initialsFrom('Elif Kaya')).toBe('EK');
  });
  it('uses the LAST word, not the second', () => {
    expect(initialsFrom('Ayse Nur Demir')).toBe('AD');
  });
  it('returns one letter for a single word', () => {
    expect(initialsFrom('Cher')).toBe('C');
  });
  it('leaves an already-uppercase Turkish dotted I alone', () => {
    expect(initialsFrom('İlker Yılmaz')).toBe('İY');
  });
  it('uppercases lowercase Turkish i to the dotted capital', () => {
    expect(initialsFrom('ilker yılmaz')).toBe('İY');
  });
  it('leaves an already-uppercase English I alone', () => {
    expect(initialsFrom('Ian Smith')).toBe('IS');
  });
  it('handles particles by taking first and last', () => {
    expect(initialsFrom('van der Berg')).toBe('VB');
  });
  it('keeps punctuation as typed', () => {
    expect(initialsFrom('J. Smith')).toBe('JS');
  });
  it('collapses runs of whitespace', () => {
    expect(initialsFrom('  Elif   Kaya  ')).toBe('EK');
  });
  it('returns empty for an empty or blank name', () => {
    expect(initialsFrom('')).toBe('');
    expect(initialsFrom('   ')).toBe('');
  });
  it('passes through scripts without case', () => {
    expect(initialsFrom('田中 太郎')).toBe('田太');
  });
  it('is code-point safe for astral characters', () => {
    expect(initialsFrom('🙂 Kaya')).toBe('🙂K');
  });
});
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

Run: `npx vitest run test/monogram.test.ts --root packages/renderer`
Expected: FAIL — "Failed to resolve import '../src/utils/monogram'"

- [ ] **Step 3: `initialsFrom` yaz**

`packages/renderer/src/utils/monogram.ts` oluştur:

```ts
/**
 * Ad-soyaddan en fazla iki baş harf üretir.
 *
 * Büyütme kuralı: karakter `toLocaleUpperCase('tr-TR')`'den geçirilir.
 * Zaten büyük olan karakterler için bu kimlik fonksiyonudur, o yüzden
 * ayrıca "zaten büyük mü" kontrolüne GEREK YOK — eklemeyin, ölü daldır.
 *
 * Sonuç tablosu (spec'ten):
 *   'İlker Yılmaz' → İY   (zaten büyük, dokunulmadı)
 *   'ilker yılmaz' → İY   (Türkçe kural)
 *   'Ian Smith'    → IS   (zaten büyük, dokunulmadı)
 *   'ian smith'    → İS   ← BİLİNEN SINIR, kabul edildi
 *
 * Son satır tamamen küçük harfle yazılmış İngilizce bir isim gerektirir;
 * insanlar kendi adını böyle yazmaz. Düz `toUpperCase()` seçilseydi 2.
 * satır `IY` çıkardı — yani gerçek müşteri kitlesinde bozulurdu.
 */
export function initialsFrom(fullName: string): string {
  const words = fullName.trim().split(/\s+/).filter(Boolean);
  if (words.length === 0) return '';

  // [...w][0] — kod noktası güvenli: 'w[0]' astral karakterlerde (emoji)
  // vekil çiftin yarısını döndürüp bozuk karakter üretirdi.
  const first = (w: string) => ([...w][0] ?? '').toLocaleUpperCase('tr-TR');

  if (words.length === 1) return first(words[0]!);
  return first(words[0]!) + first(words[words.length - 1]!);
}
```

- [ ] **Step 4: Testlerin geçtiğini gör**

Run: `npx vitest run test/monogram.test.ts --root packages/renderer`
Expected: PASS (12 test)

- [ ] **Step 5: Commit**

```bash
git add packages/renderer/src/utils/monogram.ts packages/renderer/test/monogram.test.ts
git commit -m "feat(renderer): derive monogram initials from the full name"
```

---

### Task 3: `shouldShowMonogram()` ve `monogramCell()`

**Files:**
- Modify: `packages/renderer/src/utils/monogram.ts`
- Modify: `packages/renderer/src/types.ts:53-58`
- Test: `packages/renderer/test/monogram.test.ts`

**Interfaces:**
- Consumes: `initialsFrom()` (Task 2); `readableTextOn()` (Task 1); `htmlEscape()` from `../utils/escape`; `normalizeHex()` from `../utils/color`.
- Produces:
  - `shouldShowMonogram(data: SignatureData): boolean`
  - `monogramCell(opts: { initials: string; size: number; brandHex: string; fontFamily: string; borderRadius: string }): string` — tam bir `<table>…</table>` string'i döner.

- [ ] **Step 1: Önce tipi ekle**

`packages/renderer/src/types.ts` içindeki `layout` bloğuna alan ekle:

```ts
  layout: {
    templateId: string;
    size: 'small' | 'medium' | 'large';
    iconStyle: 'filled' | 'outline' | 'mono';
    showDividers: boolean;
    /**
     * Fotoğraf yokken baş harf bloğu gösterilsin mi.
     * Alan YOKSA 'auto' sayılır — kayıtlı eski imzalar da monogram kazanır.
     * 'off' saf metin görünümü isteyenler içindir.
     * `visuals.avatarUrl` VARSA bu alan ne olursa olsun monogram basılmaz.
     */
    monogram?: 'auto' | 'off';
  };
```

- [ ] **Step 2: Failing test yaz**

`test/monogram.test.ts` içindeki MEVCUT import satırını aşağıdakiyle DEĞİŞTİR (yenisini eklersen `initialsFrom` iki kez import edilir), sonra dosyanın sonuna blokları ekle:

```ts
import { initialsFrom, shouldShowMonogram, monogramCell } from '../src/utils/monogram';
import type { SignatureData } from '../src/types';

const base: SignatureData = {
  identity: { fullName: 'Elif Kaya' },
  contact: {},
  visuals: {
    brandColor: '#7b9fd3',
    iconColor: '#7b9fd3',
    textColor: '#111827',
    mutedColor: '#6b7280',
    fontFamily: 'Arial, Helvetica, sans-serif',
  },
  social: [],
  layout: { templateId: 'classic-horizontal', size: 'medium', iconStyle: 'mono', showDividers: false },
};

describe('shouldShowMonogram', () => {
  it('is true when there is no avatar and no explicit choice', () => {
    expect(shouldShowMonogram(base)).toBe(true);
  });
  it('is false when an avatar exists', () => {
    expect(shouldShowMonogram({ ...base, visuals: { ...base.visuals, avatarUrl: 'https://cdn.mailmyra.com/a.png' } })).toBe(false);
  });
  it('is false when explicitly turned off', () => {
    expect(shouldShowMonogram({ ...base, layout: { ...base.layout, monogram: 'off' } })).toBe(false);
  });
  it('is true when explicitly auto', () => {
    expect(shouldShowMonogram({ ...base, layout: { ...base.layout, monogram: 'auto' } })).toBe(true);
  });
  it('is false when the name yields no initials', () => {
    expect(shouldShowMonogram({ ...base, identity: { fullName: '   ' } })).toBe(false);
  });
});

describe('monogramCell', () => {
  const html = monogramCell({
    initials: 'EK', size: 90, brandHex: '#7b9fd3',
    fontFamily: 'Arial, Helvetica, sans-serif', borderRadius: '4px',
  });

  it('sets the background with BOTH the attribute and the style', () => {
    expect(html).toContain('bgcolor="#7b9fd3"');
    expect(html).toContain('background-color:#7b9fd3');
  });
  it('picks a readable text colour for the brand', () => {
    expect(html).toContain('color:#000000');
  });
  it('sizes the box on both the attribute and the style', () => {
    expect(html).toContain('width="90"');
    expect(html).toContain('height="90"');
    expect(html).toContain('width:90px');
    expect(html).toContain('height:90px');
  });
  it('centres vertically the way the Word engine needs', () => {
    expect(html).toContain('valign="middle"');
    expect(html).toContain('mso-line-height-rule:exactly');
    expect(html).toContain('line-height:90px');
  });
  it('uses 40% of the box for the font size', () => {
    expect(html).toContain('font-size:36px');
  });
  it('uses the signature font, not a hardcoded one', () => {
    expect(html).toContain('Arial, Helvetica, sans-serif');
  });
  it('mirrors the radius it was given', () => {
    expect(html).toContain('border-radius:4px');
    expect(monogramCell({ initials: 'EK', size: 120, brandHex: '#7b9fd3', fontFamily: 'Arial, Helvetica, sans-serif', borderRadius: '50%' })).toContain('border-radius:50%');
  });
  it('escapes the initials', () => {
    expect(monogramCell({ initials: '<&', size: 90, brandHex: '#7b9fd3', fontFamily: 'Arial, Helvetica, sans-serif', borderRadius: '4px' })).toContain('&lt;&amp;');
  });
  it('emits no forbidden constructs', () => {
    expect(html).not.toMatch(/<div|<style|<svg|position:|display:\s*flex|<img/i);
  });
});
```

- [ ] **Step 3: Testin başarısız olduğunu gör**

Run: `npx vitest run test/monogram.test.ts --root packages/renderer`
Expected: FAIL — `shouldShowMonogram is not a function`

- [ ] **Step 4: İki fonksiyonu yaz**

`packages/renderer/src/utils/monogram.ts` dosyasının sonuna ekle:

```ts
import type { SignatureData } from '../types';
import { normalizeHex, readableTextOn } from './color';
import { htmlEscape } from './escape';

/**
 * Monogram basılacak mı: fotoğraf YOKSA, kapatılmamışsa ve addan en az bir
 * harf türetilebiliyorsa. `avatarUrl` varsa monogram fotoğrafın yerini almaz.
 */
export function shouldShowMonogram(data: SignatureData): boolean {
  if (data.visuals.avatarUrl) return false;
  if ((data.layout.monogram ?? 'auto') === 'off') return false;
  return initialsFrom(data.identity.fullName).length > 0;
}

/**
 * Marka renginde, baş harfleri ortalanmış tek hücrelik tablo.
 *
 * Görsel DEĞİL, bilerek: kurumsal Outlook dış görselleri varsayılan
 * engeller; fotoğrafı olmayan kullanıcı için eklenen fallback'in kendisi
 * engellenebilir olsaydı tam ihtiyaç anında hiçbir şey göstermezdi.
 *
 * `bgcolor` attribute'u ve `background-color` stili BİRLİKTE verilir —
 * Word motoru CSS zeminini her zaman uygulamıyor. Dikey ortalama için
 * `valign` + `mso-line-height-rule: exactly` + kutu boyuna eşit
 * `line-height` birlikte kullanılır (tek satırlık metinde bilinen yöntem).
 *
 * Punto kutunun %40'ı: Arial bold büyük harf ≈ 0.72em, iki harf ≈ 1.44em,
 * `1.44 × 0.4 = 0.576` → baş harfler kutunun ~%58'ini kaplar, her boyutta
 * rahat sığar. Oranı yükseltmek en küçük kutuda (40px) taşma riski doğurur.
 */
export function monogramCell(opts: {
  initials: string;
  size: number;
  brandHex: string;
  fontFamily: string;
  /** Şablonun KENDİ avatar yarıçapı — beşi '4px', photo-first '50%'. */
  borderRadius: string;
}): string {
  const bg = normalizeHex(opts.brandHex);
  const fg = readableTextOn(bg);
  const fontSize = Math.round(opts.size * 0.4);
  const style = [
    `width:${opts.size}px`,
    `height:${opts.size}px`,
    `background-color:${bg}`,
    `color:${fg}`,
    `font-family:${opts.fontFamily}`,
    `font-size:${fontSize}px`,
    'font-weight:bold',
    'letter-spacing:0.02em',
    'text-align:center',
    'mso-line-height-rule:exactly',
    `line-height:${opts.size}px`,
    `border-radius:${opts.borderRadius}`,
  ].join(';');

  return (
    `<table role="presentation" border="0" cellpadding="0" cellspacing="0" width="${opts.size}" ` +
    `style="width:${opts.size}px;border-collapse:collapse;"><tr>` +
    `<td align="center" valign="middle" bgcolor="${bg}" width="${opts.size}" height="${opts.size}" ` +
    `style="${style}">${htmlEscape(opts.initials)}</td>` +
    `</tr></table>`
  );
}
```

⚠️ `import` satırları dosyanın EN ÜSTÜNE taşınmalı (Task 2'de yazdığın `initialsFrom`'un üstüne), gövdenin ortasına değil.

- [ ] **Step 5: Testlerin geçtiğini gör**

Run: `npx vitest run test/monogram.test.ts --root packages/renderer`
Expected: PASS (12 + 5 + 9 = 26 test)

- [ ] **Step 6: Commit**

```bash
git add packages/renderer/src/utils/monogram.ts packages/renderer/src/types.ts packages/renderer/test/monogram.test.ts
git commit -m "feat(renderer): build the monogram cell as table markup, not an image"
```

---

### Task 4: `classic-horizontal`'a bağla (deseni kuran şablon)

**Files:**
- Modify: `packages/renderer/src/templates/classic-horizontal.ts:289-306`
- Test: `packages/renderer/test/classic-horizontal.test.ts`

**Interfaces:**
- Consumes: `shouldShowMonogram()`, `monogramCell()`, `initialsFrom()` (Task 3).
- Produces: bu şablonun `visualRows` dalı — Task 5 kalan beş şablonda AYNI deseni tekrarlar.

- [ ] **Step 1: Failing test yaz**

`test/classic-horizontal.test.ts` sonuna ekle:

```ts
describe('classic-horizontal monogram', () => {
  // SignatureData ile ACIKCA tiplenir: annotation olmadan `fontFamily`
  // `WebSafeFont` birlesimi yerine `string`e genisliyor ve `tsc --noEmit`
  // kiriliyor (vitest'in esbuild donusumu bunu yakalamaz, typecheck yakalar).
  const noPhoto: SignatureData = {
    identity: { fullName: 'Elif Kaya' },
    contact: {},
    visuals: {
      brandColor: '#7b9fd3', iconColor: '#7b9fd3', textColor: '#111827', mutedColor: '#6b7280',
      fontFamily: 'Arial, Helvetica, sans-serif',
    },
    social: [],
    layout: { templateId: 'classic-horizontal', size: 'medium' as const, iconStyle: 'mono' as const, showDividers: false },
  };

  it('shows a monogram when there is no photo', () => {
    const html = renderSignature(noPhoto, 'classic-horizontal');
    expect(html).toContain('bgcolor="#7b9fd3"');
    expect(html).toContain('>EK<');
  });
  it('shows the photo and no monogram when a photo exists', () => {
    const html = renderSignature(
      { ...noPhoto, visuals: { ...noPhoto.visuals, avatarUrl: 'https://cdn.mailmyra.com/a.png' } },
      'classic-horizontal',
    );
    expect(html).toContain('<img');
    expect(html).not.toContain('>EK<');
  });
  it('shows nothing when the monogram is turned off', () => {
    const html = renderSignature(
      { ...noPhoto, layout: { ...noPhoto.layout, monogram: 'off' as const } },
      'classic-horizontal',
    );
    expect(html).not.toContain('>EK<');
  });
  it('uses this template medium avatar box of 90px', () => {
    expect(renderSignature(noPhoto, 'classic-horizontal')).toContain('height="90"');
  });
});
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

Run: `npx vitest run test/classic-horizontal.test.ts -t monogram --root packages/renderer`
Expected: FAIL — `bgcolor="#7b9fd3"` bulunamıyor.

- [ ] **Step 3: Şablona dalı ekle**

`packages/renderer/src/templates/classic-horizontal.ts` başına import ekle:

```ts
import { initialsFrom, monogramCell, shouldShowMonogram } from '../utils/monogram';
```

`if (data.visuals.avatarUrl) { … }` bloğunun KAPANIŞINDAN hemen sonra (`logoUrl` kontrolünden ÖNCE) ekle:

```ts
  } else if (shouldShowMonogram(data)) {
    // Fotoğraf yoksa aynı kutuya monogram: bu şablonun avatar ölçeği ve
    // avatarıyla AYNI yarıçap (4px) — yeni bir tutarsızlık getirmez.
    visualRows.push(
      row(
        cell(
          monogramCell({
            initials: initialsFrom(data.identity.fullName),
            size: s.avatar,
            brandHex: data.visuals.brandColor,
            fontFamily: font,
            borderRadius: '4px',
          }),
        ),
      ),
    );
```

Yani mevcut `}` kapanışı `} else if (…) {` olur ve yeni gövde eklenir.

⚠️ `font` değişkeni bu şablonda zaten tanımlı (`const font = data.visuals.fontFamily;`). Yoksa `data.visuals.fontFamily` yaz.

- [ ] **Step 4: Testlerin geçtiğini gör**

Run: `npx vitest run test/classic-horizontal.test.ts --root packages/renderer`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/renderer/src/templates/classic-horizontal.ts packages/renderer/test/classic-horizontal.test.ts
git commit -m "feat(renderer): show a monogram in classic-horizontal when there is no photo"
```

---

### Task 5: Kalan beş şablona bağla

**Files:**
- Modify: `packages/renderer/src/templates/card-bordered.ts:104`
- Modify: `packages/renderer/src/templates/divider-columns.ts:342`
- Modify: `packages/renderer/src/templates/stacked-minimal.ts:81`
- Modify: `packages/renderer/src/templates/cta-banner.ts:270`
- Modify: `packages/renderer/src/templates/photo-first.ts:338-339`
- Test: her şablonun kendi `test/<ad>.test.ts` dosyası

**Interfaces:**
- Consumes: Task 3'ün üç fonksiyonu; Task 4'ün kurduğu desen.
- Produces: yok (son bağlama adımı).

**Şablon başına parametreler — KUTU BOYUTU VE YARIÇAP FARKLI, karıştırma:**

| şablon | avatar px (S/M/L) | borderRadius | dal deseni |
|---|---|---|---|
| `card-bordered` | 48 / 60 / 72 | `'4px'` | `if (avatarUrl) {…}` → `else if` |
| `divider-columns` | 64 / 90 / 110 | `'4px'` | `if (avatarUrl) {…}` → `else if` |
| `stacked-minimal` | 56 / 72 / 88 | `'4px'` | `if (avatarUrl) {…}` → `else if` |
| `cta-banner` | 40 / 48 / 56 | `'4px'` | üçlü operatör (aşağı bak) |
| `photo-first` | 88 / 104 / 120 | `'50%'` | üçlü operatör (aşağı bak) |

- [ ] **Step 1: Üç `if` desenli şablon için failing test yaz**

`test/card-bordered.test.ts`, `test/divider-columns.test.ts`, `test/stacked-minimal.test.ts` dosyalarının HER BİRİNE aşağıdaki bloğu ekle. `TID` ve `BOX` değerlerini o dosyanın şablonuna göre doldur:

| dosya | `TID` | `BOX` (medium) |
|---|---|---|
| `card-bordered.test.ts` | `'card-bordered'` | `60` |
| `divider-columns.test.ts` | `'divider-columns'` | `90` |
| `stacked-minimal.test.ts` | `'stacked-minimal'` | `72` |

Dosyanın import bloğuna `import type { SignatureData } from '../src/types';` ekle (yoksa).

```ts
describe('TID monogram', () => {
  // SignatureData ile ACIKCA tiplenir: annotation olmadan `fontFamily`
  // `WebSafeFont` birlesimi yerine `string`e genisliyor ve `tsc --noEmit`
  // kiriliyor (vitest'in esbuild donusumu bunu yakalamaz, typecheck yakalar).
  const noPhoto: SignatureData = {
    identity: { fullName: 'Elif Kaya' },
    contact: {},
    visuals: {
      brandColor: '#7b9fd3', iconColor: '#7b9fd3', textColor: '#111827', mutedColor: '#6b7280',
      fontFamily: 'Arial, Helvetica, sans-serif',
    },
    social: [],
    layout: { templateId: TID, size: 'medium' as const, iconStyle: 'mono' as const, showDividers: false },
  };

  it('shows a monogram when there is no photo', () => {
    const html = renderSignature(noPhoto, TID);
    expect(html).toContain('bgcolor="#7b9fd3"');
    expect(html).toContain('>EK<');
  });
  it('shows the photo and no monogram when a photo exists', () => {
    const html = renderSignature(
      { ...noPhoto, visuals: { ...noPhoto.visuals, avatarUrl: 'https://cdn.mailmyra.com/a.png' } },
      TID,
    );
    expect(html).toContain('<img');
    expect(html).not.toContain('>EK<');
  });
  it('shows nothing when the monogram is turned off', () => {
    const html = renderSignature(
      { ...noPhoto, layout: { ...noPhoto.layout, monogram: 'off' as const } },
      TID,
    );
    expect(html).not.toContain('>EK<');
  });
  it('uses this template medium avatar box', () => {
    expect(renderSignature(noPhoto, TID)).toContain('height="BOX"');
  });
});
```

- [ ] **Step 2: Üç şablona dalı ekle**

Her üç dosyanın başına import ekle:

```ts
import { initialsFrom, monogramCell, shouldShowMonogram } from '../utils/monogram';
```

Her birinde `if (data.visuals.avatarUrl) { … }` bloğunun kapanış `}`'sini `} else if (…) {` yapıp gövdeyi ekle:

```ts
  } else if (shouldShowMonogram(data)) {
    // Fotoğraf yoksa aynı kutuya monogram: bu şablonun avatar ölçeği ve
    // avatarıyla AYNI yarıçap (4px) — yeni bir tutarsızlık getirmez.
    visualRows.push(
      row(
        cell(
          monogramCell({
            initials: initialsFrom(data.identity.fullName),
            size: s.avatar,
            brandHex: data.visuals.brandColor,
            fontFamily: data.visuals.fontFamily,
            borderRadius: '4px',
          }),
        ),
      ),
    );
```

⚠️ Dizi adı şablona göre değişebilir (`visualRows` vs. başka bir ad) — `if (data.visuals.avatarUrl)` bloğunun içinde hangi diziye `push` ediliyorsa AYNI diziyi kullan.

- [ ] **Step 3: Üçünün testlerini koş**

Run: `npx vitest run test/card-bordered.test.ts test/divider-columns.test.ts test/stacked-minimal.test.ts --root packages/renderer`
Expected: PASS

- [ ] **Step 4: `cta-banner` için failing test yaz ve bağla**

Test: Task 4 deseni, `templateId: 'cta-banner'`, medium kutu `height="48"`.

`cta-banner.ts:270` üçlü operatör kullanıyor ve else tarafı `: '';` (satır 287). Yani yapı `const avatarCell = data.visuals.avatarUrl ? cell(…) : '';`. Sondaki `: '';` kısmını şununla değiştir:

```ts
    : shouldShowMonogram(data)
      ? cell(
          monogramCell({
            initials: initialsFrom(data.identity.fullName),
            size: s.avatar,
            brandHex: data.visuals.brandColor,
            fontFamily: data.visuals.fontFamily,
            borderRadius: '4px',
          }),
          { valign: 'top', width: s.avatar, style: { 'padding-right': `${s.gap}px` } },
        )
      : '';
```

Run: `npx vitest run test/cta-banner.test.ts --root packages/renderer` → PASS

- [ ] **Step 5: `photo-first` için failing test yaz ve bağla — YARIÇAP `'50%'`**

Test: Task 4 deseni, `templateId: 'photo-first'`, medium kutu `height="104"`, ARTI:

```ts
  it('mirrors this template round avatar with a round monogram', () => {
    expect(renderSignature(noPhoto, 'photo-first')).toContain('border-radius:50%');
  });
```

`photo-first.ts:339` `const leftCell = hasAvatar ? cell(…) : '';` yapısında (else tarafı satır 356). Sondaki `: '';` kısmını şununla değiştir:

```ts
    : shouldShowMonogram(data)
      ? cell(
          monogramCell({
            initials: initialsFrom(data.identity.fullName),
            size: s.avatar,
            brandHex: data.visuals.brandColor,
            fontFamily: data.visuals.fontFamily,
            borderRadius: '50%',
          }),
          { valign: 'top', width: s.avatar, style: { 'padding-right': `${s.gap}px` } },
        )
      : '';
```

**`borderRadius: '50%'`** — bu şablonun avatarı yuvarlak (`photo-first.ts:346`), kare monogram yanına yakışmaz. Diğer beş şablon `'4px'` verir.

⚠️ **AYRICA `photo-first.ts:376` DÜZELTİLECEK — yoksa tablo düzeni bozulur.**

Logo hücresi şu an `...(hasAvatar ? { colspan: 2 } : {})` diyor. Gerekçe: avatar varken ana satır İKİ hücreli (avatar + içerik), logo da ikisini kaplamalı. Monogram avatarın yerine geçtiğinde ana satır YİNE iki hücreli olur — ama `hasAvatar` false kalır, `colspan` uygulanmaz ve logo tek kolona sıkışır.

Koşulu "üstte iki hücreli bir satır var mı" sorusuna çevir:

```ts
            ...(hasAvatar || shouldShowMonogram(data) ? { colspan: 2 } : {}),
```

Ve bunu kilitleyen bir test ekle:

```ts
  it('keeps the logo spanning both columns when a monogram replaces the photo', () => {
    const html = renderSignature(
      { ...noPhoto, visuals: { ...noPhoto.visuals, logoUrl: 'https://cdn.mailmyra.com/l.png' } },
      'photo-first',
    );
    expect(html).toContain('colspan="2"');
  });
```

Bu, `classic-horizontal`'da yakalanan hatanın aynısının bu şablondaki hâli: avatar varlığına bağlanmış bir yerleşim kararı, monogram gelince yanlış tarafa düşüyor.

Run: `npx vitest run test/photo-first.test.ts --root packages/renderer` → PASS

- [ ] **Step 6: Tüm testleri koş**

Run: `npm test -w packages/renderer`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add packages/renderer/src/templates packages/renderer/test
git commit -m "feat(renderer): show the monogram in the remaining five templates

photo-first passes 50% so the monogram matches its round avatar; the
other four pass 4px, matching theirs."
```

---

### Task 6: Çapraz şablon güvence testleri

**Files:**
- Modify: `packages/renderer/test/guardrails.test.ts:16-20`
- Create: `packages/renderer/test/monogram-coverage.test.ts`

**Interfaces:**
- Consumes: `TEMPLATE_IDS` from `../src/render`; tüm önceki tasklar.
- Produces: yok (son task).

- [ ] **Step 1: Kapsam testini yaz — bu spec'in ÇIKIŞ NOKTASINI kilitler**

`packages/renderer/test/monogram-coverage.test.ts` oluştur:

```ts
import { describe, it, expect } from 'vitest';
import { renderSignature, TEMPLATE_IDS } from '../src/render';
import { contrastRatio } from '../src/utils/color';
import type { SignatureData } from '../src/types';

// Bu spec'in cikis noktasi: fotograf/logo/el imzasi olmadan ALTI sablon da
// sifir <img> uretiyordu, yani sablon secimi anlamsizlasiyordu. Monogram bir
// <img> DEGIL (bilerek — Outlook gorselleri engeller), o yuzden bu test
// <img> saymaz; fotografsiz ciktida bir monogram hucresinin VARLIGINI olcer.
const bare: SignatureData = {
  identity: { fullName: 'Elif Kaya' },
  contact: { email: 'elif@voldi.net' },
  visuals: {
    brandColor: '#7b9fd3', iconColor: '#7b9fd3', textColor: '#111827', mutedColor: '#6b7280',
    fontFamily: 'Arial, Helvetica, sans-serif',
  },
  social: [],
  layout: { templateId: '', size: 'medium', iconStyle: 'mono', showDividers: false },
};

for (const templateId of TEMPLATE_IDS) {
  describe(`monogram coverage: ${templateId}`, () => {
    const html = renderSignature({ ...bare, layout: { ...bare.layout, templateId } }, templateId);

    it('gives a photo-less signature a visual anchor', () => {
      expect(html).toContain('bgcolor="#7b9fd3"');
      expect(html).toContain('>EK<');
    });
    it('keeps the monogram out of the markup when turned off', () => {
      const off = renderSignature(
        { ...bare, layout: { ...bare.layout, templateId, monogram: 'off' } },
        templateId,
      );
      expect(off).not.toContain('>EK<');
    });
    it('drops the monogram when a photo is supplied', () => {
      const withPhoto = renderSignature(
        { ...bare, layout: { ...bare.layout, templateId }, visuals: { ...bare.visuals, avatarUrl: 'https://cdn.mailmyra.com/a.png' } },
        templateId,
      );
      expect(withPhoto).not.toContain('>EK<');
    });
    it('never lets the monogram fall below 4.5:1 contrast', () => {
      // Adini tutan test: rengi OKUYUP kontrasti gercekten hesaplar.
      // Yalnizca "siyah ya da beyaz mi" diye bakmak bu iddiayi kanitlamaz.
      const cellMatch = html.match(/bgcolor="(#[0-9a-f]{6})"[^>]*style="([^"]*)"/i);
      expect(cellMatch).not.toBeNull();
      const bg = cellMatch![1]!;
      const fg = cellMatch![2]!.match(/(?:^|;)color:(#[0-9a-f]{6})/i)![1]!;
      expect(contrastRatio(bg, fg)).toBeGreaterThanOrEqual(4.5);
    });
  });
}
```

- [ ] **Step 2: Koş**

Run: `npx vitest run test/monogram-coverage.test.ts --root packages/renderer`
Expected: PASS (6 şablon × 4 = 24 test)

- [ ] **Step 3: Guardrail suite'ine fotoğrafsız mod ekle**

`test/guardrails.test.ts` içindeki `MODES` dizisine üçüncü bir mod eklemek YERİNE, dosyanın sonuna ayrı bir blok ekle (mevcut fixture döngüsünü bozmamak için):

```ts
// Bu blok, monogram dalını fixture bileşimine güvenmeden AÇIKÇA sabitler
// (MODES döngüsündeki `minimal`/`noLogo` fixture'larının zaten avatarUrl'ü
// yok, yani dal orada da tetikleniyor — ama MODES'un kapsamı fixture
// listesi değişince sessizce daralabilir; burası her zaman fotoğrafsız bir
// SignatureData ile çağrıldığı için o riski taşımaz).
// ⚠️ Final review düzeltmesi (bkz. guardrails.test.ts): önceki yorum
// "fixture'larda hiç tetiklenmiyor" diyordu — bu YANLIŞTI, yukarıdaki
// parantez doğrusu.
describe('guardrails: monogram branch', () => {
  for (const templateId of TEMPLATE_IDS) {
    it(`${templateId} emits no forbidden constructs without a photo`, () => {
      const html = renderSignature(
        {
          identity: { fullName: 'Elif Kaya' },
          contact: { email: 'elif@voldi.net' },
          visuals: {
            brandColor: '#7b9fd3', iconColor: '#7b9fd3', textColor: '#111827', mutedColor: '#6b7280',
            fontFamily: 'Arial, Helvetica, sans-serif',
          },
          social: [],
          layout: { templateId, size: 'medium', iconStyle: 'mono', showDividers: false },
        },
        templateId,
      );
      expect(html).not.toMatch(/<div|<style|<svg|position:\s*(absolute|fixed|relative)|display:\s*flex|display:\s*grid|float:/i);
    });
  }
});
```

- [ ] **Step 4: Tüm testleri koş**

Run: `npm test -w packages/renderer`
Expected: PASS

- [ ] **Step 5: Typecheck**

Run: `npm run typecheck -w packages/renderer`
Expected: hatasız

- [ ] **Step 6: Commit**

```bash
git add packages/renderer/test
git commit -m "test(renderer): lock the finding that started this — no photo, no visual"
```

---

## Yayın şartı (kod bittikten SONRA, ayrı iş)

CLAUDE.md: şablon başına 6-istemci test matrisi yayın şartıdır. Monogram altı şablonu da etkilediği için matris yeniden koşulmalı. Öncelikli bakılacaklar:

1. **Outlook Classic'te monogram kare, ortalanmış ve tam boyutta mı** — `valign` + `mso-line-height-rule` kombinasyonunun gerçekten tuttuğu tek yer orası.
2. **`photo-first`'te 120px'lik renk bloğu** — en riskli vaka.
3. **Görsel engelli Outlook'ta monogram görünüyor mu** — bu özelliğin varlık sebebi; doğrulanmadan "tamam" denmez.
4. CTA buton metni siyaha döndü (Task 1) — altı şablonda okunur mu.

Not: `divider-columns`, `photo-first`, `cta-banner` matristen HİÇ geçmemişti; bu tur ikisi birlikte kapatılabilir.
